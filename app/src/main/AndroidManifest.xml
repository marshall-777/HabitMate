<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.habitmate">

    <!-- Разрешение на доступ в интернет -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HabitMate"
        tools:targetApi="31">

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="Вход">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RegisterActivity"
            android:exported="true"
            android:label="Регистрация" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="HabitMate" />

        <activity
            android:name=".HabitDetailActivity"
            android:exported="false"
            android:label="Детали привычки" />

        <!-- 🔽 Добавлено: экран прогресса привычки -->
        <activity
            android:name=".HabitProgressActivity"
            android:exported="false"
            android:label="Прогресс привычки" />
    </application>
</manifest>